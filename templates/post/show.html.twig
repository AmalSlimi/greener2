{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}
{% block  crud %}
	<div class="main-header">
		<a class="menu-link-main" href="#">Acceuil admin</a>
			<div class="header-menu">
				<a class="main-header-link" href="{{ path('app_post_new') }}">Ajouter</a>
				<a class="main-header-link" href="{{ path('app_post_index') }}">Afficher</a>
				<a class="main-header-link" href="{{ path('app_commentaires_index') }}">Commentaires</a>
				<a class="main-header-link" href="{{ path('app_search') }}">Rechercher</a>
			</div>
	</div>
{% endblock %}
{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPost</th>
                <td>{{ post.idPost }}</td>
            </tr>
            <tr>
                <th>IdEntreprise</th>
                <td>{{ post.idEntreprise }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ post.titre }}</td>
            </tr>
            <tr>
                <th>Typedecontenu</th>
                <td>{{ post.typedecontenu }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ post.contenu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ post.date ? post.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ post.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_post_index') }}">back to list</a>

    <a href="{{ path('app_post_edit', {'idPost': post.idPost}) }}">edit</a>

    {{ include('post/_delete_form.html.twig') }}
{% endblock %}
