{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}
{% block  crud %}
	<div class="main-header">
		<a class="menu-link-main" href="#">Acceuil admin</a>
			<div class="header-menu">
				<a class="main-header-link" href="{{ path('app_panier_new') }}">Ajouter </a>
				<a class="main-header-link" href="{{ path('app_commande_index') }}">Afficher </a>
			</div>
	</div>
{% endblock %}
{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Panierid</th>
                <td>{{ panier.panierid }}</td>
            </tr>
            <tr>
                <th>Clientid</th>
                <td>{{ panier.clientid }}</td>
            </tr>
            <tr>
                <th>Produitid</th>
                <td>{{ panier.produitid }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ panier.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ panier.prix }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ panier.total }}</td>
            </tr>
            <tr>
                <th>Nomproduit</th>
                <td>{{ panier.nomproduit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_panier_index') }}">back to list</a>

    <a href="{{ path('app_panier_edit', {'panierid': panier.panierid}) }}">edit</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
